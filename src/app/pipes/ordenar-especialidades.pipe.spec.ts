import { OrdenarEspecialidadesPipe } from './ordenar-especialidades.pipe';

describe('OrdenarEspecialidadesPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarEspecialidadesPipe();
    expect(pipe).toBeTruthy();
  });
});
